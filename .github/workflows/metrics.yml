name: Metrics
on:
  # Her gece yarısı otomatik güncelleme
  schedule: [{cron: "0 0 * * *"}]
  # El ile (manuel) tetikleme izni
  workflow_dispatch:
jobs:
  github-metrics:
    runs-on: ubuntu-latest
    # Bu kısım, oluşturulan görseli depoya geri yazabilmek için şarttır:
    permissions:
      contents: write
    steps:
      - name: 📊 Organizasyon Aktivite Grafiği Oluştur
        uses: lowlighter/metrics@latest
        with:
          # GitHub Secrets'a eklediğin token adı
          token: ${{ secrets.METRICS_TOKEN }}
          
          # Organizasyon ayarları
          user: mustafahallal
          base: "" 
          
          # KRİTİK AYAR: Organizasyon bağlamını burada tanımlıyoruz
          # "Unsupported context" hatasını bu satır çözer.
          user_type: organization
          
          # Takvim eklentisi ayarları
          plugin_isocalendar: yes
          plugin_isocalendar_duration: full-year
          
          # Oluşturulacak dosyanın adı (README'deki linkle aynı olmalı)
          filename: github-metrics.svg
